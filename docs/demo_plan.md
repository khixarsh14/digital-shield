# Digital Shield — Demo Plan

## Future WhatsApp presentation concept

Show a slide animation labeled **Future integration concept — not implemented**:

```text
Suspicious WhatsApp message
        ↓
User forwards to Digital Shield
        ↓
Digital Shield investigates
        ↓
Risk result appears
```

This animation is a presentation concept, not an implemented system or live integration.
Once the browser verification flow is complete, transition with:

> “For the hackathon prototype, we implemented the verification experience as a browser application.”

Until then, describe it as planned: currently the frontend is a Vite starter and
the backend returns a placeholder.

## Live browser demo

Run all live scenarios only in the browser application. Never present preserved
legacy chat-demo material as an implemented channel.

After the required capabilities are implemented, rehearse:

1. Fake internship: scam signals, traffic-light result, and recommended actions.
2. Government claim with suspicious URL: relevant tools and supporting sources.
3. Ambiguous message: missing-information question and follow-up response.
4. Screenshot: extracted content and investigation when vision is ready.

Show English/Urdu and browser speech where supported. Only claim tools ran when
they actually did; fixture results and the current placeholder are not verification.

## Readiness

- Verify the browser/backend connection and fixed scenario inputs.
- Check loading, validation, network failures, mobile layout, and Urdu readability.
- Confirm unverified does not mean false and green is not a safety guarantee.
- Label unavailable features as pending and any recording as a recording.

The foundation can currently be demonstrated through `/docs`; the live browser
verification demo is not ready yet.
